# --- Основные компоненты RAG-системы ---
# Клиент для векторной базы данных Qdrant
qdrant-client==1.9.0

# Фреймворк для работы с языковыми моделями (используется для разбиения текста)
langchain==0.2.1

# Модель для создания векторных представлений (эмбеддингов)
sentence-transformers==3.0.1


# --- Обработка документов ---
# Основная библиотека для извлечения текста из разных форматов
# [docx] добавляет поддержку файлов Microsoft Word .docx
unstructured[docx]==0.14.5

# Библиотека для извлечения текста из PDF-файлов
pypdf==4.2.0


# --- Системные и служебные зависимости ---
# Библиотека для выполнения HTTP-запросов (связь с OpenAI на ВМ)
requests==2.32.3

# Основная библиотека для машинного обучения, зависимость для sentence-transformers
# Укажите версии, совместимые с вашей версией CUDA, если используете GPU
# Пример для CPU-only:
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# Зависимость для конвертации .doc в .docx на ОС Windows
# Установка может потребовать прав администратора
# Для других ОС (Linux, macOS) эта зависимость не используется и будет проигнорирована
pywin32==306; sys_platform == 'win32'