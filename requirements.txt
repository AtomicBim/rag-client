# ================================================
# RAG-система для обработки документов и поиска
# ================================================

# --- Основные компоненты RAG-системы ---
# Клиент для векторной базы данных Qdrant
qdrant-client==1.9.0

# Фреймворк для работы с языковыми моделями (используется для разбиения текста)
langchain==0.2.1

# Модель для создания векторных представлений (эмбеддингов)
sentence-transformers==3.0.1

# --- FastAPI и веб-сервер ---
# Современный веб-фреймворк для создания API
fastapi==0.116.1

# ASGI сервер для запуска FastAPI приложений  
uvicorn==0.35.0

# --- Обработка документов ---
# Основная библиотека для извлечения текста из разных форматов
# [docx] добавляет поддержку файлов Microsoft Word .docx
unstructured[docx]==0.14.5

# Библиотека для извлечения текста из PDF-файлов (совместимая версия)
pypdf==4.2.0

# --- Машинное обучение ---
# PyTorch для работы с нейронными сетями (автоматически устанавливается с sentence-transformers)
# ВАЖНО: На Windows может потребоваться Microsoft Visual C++ Redistributable
# Скачать можно здесь: https://aka.ms/vs/16/release/vc_redist.x64.exe
torch>=2.0.0

# Научные вычисления
numpy>=1.21.0
scipy>=1.7.0

# --- Системные и служебные зависимости ---
# Библиотека для выполнения HTTP-запросов
requests==2.32.3

# Зависимость для конвертации .doc в .docx на ОС Windows
# Установка может потребовать прав администратора
pywin32==306; sys_platform == 'win32'

# --- Дополнительные зависимости для разработки ---
# Типизация для Python < 3.9
typing-extensions>=4.0.0

# Логирование и обработка данных
python-dotenv>=1.0.0  # Поддержка .env файлов для конфигурации

# --- Примечания по установке ---
# 
# 1. На Windows может потребоваться Microsoft Visual C++ Redistributable:
#    https://aka.ms/vs/16/release/vc_redist.x64.exe
#
# 2. Для GPU поддержки установите соответствующую версию PyTorch:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 3. Установка всех зависимостей:
#    pip install -r requirements.txt
#
# 4. Проверка установки:
#    python -c "import qdrant_client, langchain, sentence_transformers, unstructured, pypdf, fastapi, uvicorn; print('✅ Все пакеты установлены!')"